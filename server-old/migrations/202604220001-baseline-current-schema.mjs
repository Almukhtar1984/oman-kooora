export const up = async () => {
    // Baseline marker for the schema that existed before migrations were added.
};

export const down = async () => {
    // Baseline rollback intentionally does nothing.
};
